# Scientific package requirements used for cluster training.
# Compute Canada supplies several packages with a +computecanada build suffix;
# the runtime manifest records those full installed versions for every run.
numpy==2.2.2
pandas==2.2.3
pyarrow==19.0.1
scipy==1.15.1
scikit-learn==1.6.1
# Alliance module: rdkit/2024.09.6. The frozen split was generated locally
# with rdkit==2024.9.4; cluster does not regenerate the split.
PyYAML==6.0.3
xgboost==3.2.0
joblib==1.5.3
torch==2.12.1
torch_geometric==2.7.0
pytest==9.0.2
matplotlib==3.10.0
